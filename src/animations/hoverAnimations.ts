// Hover and interaction animations

// Button hover and tap animations
export const buttonVariants = {
  hover: { scale: 1.05 },
  tap: { scale: 0.95 }
};

// Link hover animation with arrow movement
export const linkHoverVariants = {
  hover: { x: 5 },
  transition: { duration: 0.2 }
};

// Image or element scale on hover
export const scaleHoverVariants = {
  hover: { scale: 1.05 },
  transition: { duration: 0.3 }
};

// Badge hover animation
export const badgeHoverVariants = {
  hover: { scale: 1.1 },
  transition: { duration: 0.2 }
};

// Technology tag hover animation
export const tagHoverVariants = {
  hover: { scale: 1.02 },
  transition: { duration: 0.2 }
};

// Input focus animation
export const inputFocusVariants = {
  focus: { boxShadow: "0 0 0 3px rgba(59, 130, 246, 0.3)" }
};
